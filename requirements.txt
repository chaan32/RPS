# Python 의존성 (운영 + 일부 도구).
# Docker 이미지 빌드 + 로컬 venv 모두에서 사용.
# environment.yml 의 pip 섹션에서 추출 (학습/노트북 전용은 제외).

# ── Web / API ─────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.4
pydantic-settings>=2.0.0
eval_type_backport==0.2.0

# ── DB / MQTT ─────────────────────────────────────────────────────────
sqlalchemy==2.0.30
asyncpg==0.29.0
greenlet==3.0.3
aiomqtt==2.3.0
redis==5.0.8
python-dotenv==1.0.1
loguru==0.7.3

# ── LLM ───────────────────────────────────────────────────────────────
google-generativeai==0.8.3
httpx==0.28.1

# ── Vision / Detection ────────────────────────────────────────────────
numpy>=1.24.0
opencv-python==4.10.0.84
torch>=2.0.0
torchvision>=0.15.0
ultralytics==8.4.38
lap>=0.5.12

# ── Audio / YAMNet ────────────────────────────────────────────────────
# tensorflow >= 2.16 : Linux ARM64 (Apple Silicon Docker) wheel 제공 보장.
# ARM64 환경에서는 자동으로 CPU 변종 (CUDA 라이브러리 미포함).
# x86_64 + GPU 환경이면 그대로 두면 GPU 라이브러리 자동 포함.
tensorflow>=2.16.0
tensorflow-hub>=0.13.0
soundfile>=0.12.0
librosa>=0.10.0
scikit-learn>=1.3.0
scipy>=1.11.0
pydub>=0.25.1
